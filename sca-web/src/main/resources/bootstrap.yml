spring:
  application:
    name: sca-web
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.235.128:8848
        username: nacos
        password: nacos
      config:
        server-addr: 192.168.235.128:8848
        username: nacos
        password: nacos
        file-extension: yml
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}

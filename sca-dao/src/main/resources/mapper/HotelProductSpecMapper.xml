<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opensabre.admin.dao.mapper.HotelProductSpecMapper">

    <!-- 查询某商品的所有规格列表 -->
    <select id="selectByProductId" resultType="com.opensabre.admin.dao.entity.po.HotelProductSpec">
        SELECT * FROM hotel_product_spec
        WHERE product_id = #{productId}
        ORDER BY sort ASC
    </select>

    <!-- 扣减库存（乐观锁，stock >= quantity 才更新） -->
    <update id="deductStock">
        UPDATE hotel_product_spec
        SET stock = stock - #{quantity}
        WHERE id = #{specId} AND stock >= #{quantity}
    </update>

    <!-- 归还库存 -->
    <update id="restoreStock">
        UPDATE hotel_product_spec
        SET stock = stock + #{quantity}
        WHERE id = #{specId}
    </update>

</mapper>

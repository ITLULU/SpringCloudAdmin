<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opensabre.admin.dao.mapper.HotelTripMapper">

    <!-- 查询用户在某酒店是否有时间重叠的有效行程 -->
    <select id="selectOverlappingTrips" resultType="com.opensabre.admin.dao.entity.po.HotelTrip">
        SELECT * FROM hotel_trip
        WHERE user_id = #{userId}
          AND hotel_id = #{hotelId}
          AND status = 1
          AND check_in_date &lt; #{checkOutDate}
          AND check_out_date &gt; #{checkInDate}
    </select>

    <!-- 查询用户的有效行程列表 -->
    <select id="selectByUserId" resultType="com.opensabre.admin.dao.entity.po.HotelTrip">
        SELECT * FROM hotel_trip
        WHERE user_id = #{userId}
        ORDER BY check_in_date DESC
    </select>

    <!-- 查询用户在某酒店当前是否有有效入住（今天日期在入住范围内） -->
    <select id="selectActiveTrip" resultType="com.opensabre.admin.dao.entity.po.HotelTrip">
        SELECT * FROM hotel_trip
        WHERE user_id = #{userId}
          AND hotel_id = #{hotelId}
          AND status = 1
          AND check_in_date &lt;= CURDATE()
          AND check_out_date &gt;= CURDATE()
        LIMIT 1
    </select>

</mapper>

spring:
  application:
    name: sca-order
  cloud:
    nacos:
      discovery:
        server-addr: 47.116.45.17:8848
        username: ${nacos.username}
        password: ${nacos.password}
      config:
        refresh-enabled: false
        server-addr: 47.116.45.17:8848
        username: ${nacos.username}
        password: ${nacos.password}
        file-extension: yml
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}
        namespace: ""
        # 共享配置
#        shared-configs:
#          - data-id: sca-common.yml
#            group: SHARED_GROUP
#            refresh: false
#        extensionConfigs:
#          - data-id: sca-order.yml
#            group: DEFAULT_GROUP
#            refresh: false
